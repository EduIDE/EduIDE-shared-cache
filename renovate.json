{
  "$schema": "https://docs.renovatebot.com/renovate-schema.json",
  "extends": ["local>EduIDE/.github:renovate-config"],
  "postUpdateOptions": ["helmUpdateSubChartArchives"],
  "packageRules": [
    {
      "description": "chart/values.yaml leaves image.tag empty on purpose - the tag defaults to the chart version at release time, so there is nothing for Renovate to update.",
      "matchManagers": ["helm-values"],
      "matchPackageNames": ["ghcr.io/eduide/eduide-shared-cache/gradle-cache"],
      "enabled": false
    },
    {
      "description": "Vendored subchart archives are regenerated by helm dependency update, not managed directly.",
      "matchFileNames": ["chart/charts/**"],
      "enabled": false
    }
  ]
}
